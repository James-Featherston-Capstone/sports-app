import { Button } from "../ui/button";

const Filter = () => {
  return (
    <article className="m-3 flex">
      <Button>Location</Button>
      <Button>Sport</Button>
    </article>
  );
};

export default Filter;
